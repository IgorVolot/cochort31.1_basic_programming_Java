package practice.abstract_class;

public interface House {

    // принять на довольство
    boolean addPet();

    // снять с довольства
    Pet removePet(Pet pet);

    // накупить всем еду


    // гигиена
}
